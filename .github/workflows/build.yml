---
name: Deploy gh pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      ################################
      # Setup node
      ################################
      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: "yarn"
      - name: Install requirements
        run: yarn install

      ################################
      # Build & deploy
      ################################
      - run: |
          npm run build
          echo "www.thedataengineeringbook.online" > build/CNAME

      - name: Deploy site
        with:
          build-dir: build
          deploy-branch: gh-pages
        uses: NickSchimek/deploy_jamstack_action@main
